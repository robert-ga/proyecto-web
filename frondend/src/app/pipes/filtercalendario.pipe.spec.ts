import { FiltercalendarioPipe } from './filtercalendario.pipe';

describe('FiltercalendarioPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltercalendarioPipe();
    expect(pipe).toBeTruthy();
  });
});
