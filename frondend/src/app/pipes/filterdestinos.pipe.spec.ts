import { FilterdestinosPipe } from './filterdestinos.pipe';

describe('FilterdestinosPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterdestinosPipe();
    expect(pipe).toBeTruthy();
  });
});
